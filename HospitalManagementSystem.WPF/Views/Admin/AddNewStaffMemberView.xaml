<Window x:Class="HospitalManagementSystem.WPF.Views.Admin.AddNewStaffMemberView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HospitalManagementSystem.WPF.Views.Admin"
        mc:Ignorable="d"
        Title="Add New Staff Member" Height="450" Width="800"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- Form Fields -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Name" Margin="0,0,0,2"/>
            <TextBox Width="300" Margin="0,0,0,8"/>

            <TextBlock Text="Birthdate" Margin="0,0,0,2"/>
            <DatePicker Width="300" Margin="0,0,0,8"/>

            <TextBlock Text="Position" Margin="0,0,0,2"/>
            <TextBox Width="300"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="75" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="75" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
