<UserControl x:Class="HospitalManagementSystem.WPF.Views.StaffManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HospitalManagementSystem.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:StaffManagementViewModel, IsDesignTimeCreatable=True}">


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Staff Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <DataGrid ItemsSource="{Binding StaffMembers}" 
                          SelectedItem="{Binding SelectedStaffMember}"
                          Height="350"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                        <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                        <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="Auto"/>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Load Staff" Command="{Binding LoadStaffCommand}"/>
                    <Button Content="Delete Staff" Command="{Binding DeleteStaffCommand}"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Staff Details" FontWeight="Bold" Margin="0,0,0,10"/>

                    <Label Content="First Name:"/>
                    <TextBox Text="{Binding CurrentStaffMember.FirstName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                    <Label Content="Last Name:"/>
                    <TextBox Text="{Binding CurrentStaffMember.LastName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                    <Label Content="Username:"/>
                    <TextBox Text="{Binding CurrentStaffMember.Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                    <Label Content="Password:"/>
                    <TextBox Text="{Binding CurrentStaffMember.Password, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                    <Label Content="Role:"/>
                    <ComboBox ItemsSource="{Binding StaffRoles}" 
                              SelectedItem="{Binding CurrentStaffMember.Role}"
                              Margin="5"/>
                    <Label Content="Date of Birth:"/>
                    <DatePicker SelectedDate="{Binding CurrentStaffMember.DateOfBirth, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                Margin="5"/>
                    <Label Content="Phone Number:"/>
                    <TextBox Text="{Binding CurrentStaffMember.PhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                    <Label Content="Email:"/>
                    <TextBox Text="{Binding CurrentStaffMember.Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Content="Add Staff" Command="{Binding AddStaffCommand}"/>
                        <Button Content="Update Staff" Command="{Binding UpdateStaffCommand}"/>
                        <Button Content="Clear Form" Command="{Binding ClearFormCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>