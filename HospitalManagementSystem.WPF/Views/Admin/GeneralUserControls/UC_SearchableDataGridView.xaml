<UserControl x:Class="HospitalManagementSystem.WPF.Views.Admin.GeneralUserControls.UC_SearchableDataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBox
                Width="200"
                VerticalContentAlignment="Center"
                Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                ToolTip="{Binding SearchToolTip}" />
            <Button
                Content="Search"
                Command="{Binding SearchCommand}"
                Margin="5,0,0,0"/>
            <Button Content="Filter Options" Width="100" Margin="5,0,10,0"
                    Command="{Binding FilterOptionsCommand}"/>
        </StackPanel>

        <DataGrid
            x:Name="MyDataGrid"
            Grid.Row="1"
            ItemsSource="{Binding ItemsSource}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            Margin="5"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto">
            <!-- The DataGrid will be populated with columns programmatically -->
        </DataGrid>
    </Grid>
</UserControl>