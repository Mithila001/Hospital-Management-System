<UserControl x:Class="HospitalManagementSystem.WPF.Views.Admin.GeneralUserControls.SearchableDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HospitalManagementSystem.WPF.Views.Admin.GeneralUserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Beige">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBox
        Width="200"
        VerticalContentAlignment="Center"
        Text="{Binding SearchQuery, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
        ToolTip="{Binding SearchToolTip, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Button
        Content="Search"
        Command="{Binding SearchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
        Margin="5,0,0,0"/>
            <Button
        Content="{Binding FilterButtonText, RelativeSource={RelativeSource AncestorType=UserControl}}"
        Command="{Binding FilterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
        Margin="5,0,0,0"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid
      Grid.Row="1"
      x:Name="PART_DataGrid"
      ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
      SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}}"
      AutoGenerateColumns="False"
      CanUserAddRows="False"
      Margin="5"
      VerticalScrollBarVisibility="Auto"
      HorizontalScrollBarVisibility="Auto">
            <!-- Columns will be injected in code-behind -->
        </DataGrid>
    </Grid>
</UserControl>
